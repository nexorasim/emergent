# GitHub CLI Setup

## Install GitHub CLI
```bash
# Windows (via winget)
winget install --id GitHub.cli

# Or download from: https://cli.github.com/
```

## After Installation
```bash
gh auth login
```

## Alternative - Use Git directly
```bash
git remote -v
git push origin main
```

**Status**: GitHub CLI not installed - use git commands or install gh CLI