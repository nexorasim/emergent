/**
 * Anniversary.js - 4th Anniversary Free eSIM Campaign
 * ESIM MYANMAR COMPANY LIMITED
 * Enterprise-grade free eSIM redemption with QR-less provisioning
 * Zero emoji - Professional telecom design
 */

import React, { useState } from 'react';
import { motion } from 'framer-motion';
import { Helmet } from 'react-helmet-async';

const Anniversary = () => {
  const [step, setStep] = useState('landing'); // landing, ios, android, qr

  const ESIM_LPA_STRING = 'LPA:1$rsp.truphone.com$QR-G-5C-21K-1CHHCG1';
  const IOS_PROVISION_URL = `https://esimsetup.apple.com/esim_qrcode_provisioning?carddata=${ESIM_LPA_STRING}`;
  const ANDROID_PROVISION_URL = `https://esimsetup.android.com/esim_qrcode_provisioning?carddata=${ESIM_LPA_STRING}`;

  const handleDirectInstall = (url) => {
    window.location.href = url;
  };

  const copyToClipboard = (text) => {
    navigator.clipboard.writeText(text);
  };

  return (
    <>
      <Helmet>
        <title>4th Anniversary - Free eSIM | eSIM Myanmar</title>
        <meta name="description" content="Celebrate 4 years of eSIM Myanmar. Get your free eSIM instantly with no registration required. iOS and Android supported." />
      </Helmet>

      <div className="min-h-screen py-20 relative overflow-hidden">
        {/* Background */}
        <div className="fixed inset-0 pointer-events-none">
          <div style={{
            backgroundImage: 'radial-gradient(circle at 30% 20%, rgba(0, 255, 255, 0.1) 0%, transparent 50%), radial-gradient(circle at 70% 80%, rgba(139, 92, 246, 0.08) 0%, transparent 50%)'
          }} className="absolute inset-0" />
        </div>

        <div className="container mx-auto px-4 relative z-10">
          {/* Hero Section */}
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6 }}
            className="text-center mb-16"
          >
            <div className="inline-flex items-center gap-2 px-4 py-2 rounded-full mb-6"
              style={{ background: 'rgba(139, 92, 246, 0.15)', border: '1px solid rgba(139, 92, 246, 0.3)' }}>
              <span className="w-2 h-2 rounded-full bg-purple-400 animate-pulse" />
              <span className="text-purple-400 text-sm font-semibold">4th Anniversary Special</span>
            </div>

            <h1 className="text-4xl sm:text-5xl lg:text-6xl font-bold mb-6">
              <span style={{
                background: 'linear-gradient(135deg, #8B5CF6 0%, #EC4899 50%, #00FFFF 100%)',
                WebkitBackgroundClip: 'text',
                WebkitTextFillColor: 'transparent'
              }}>
                Celebrating 4 Years
              </span>
              <br />
              <span className="text-white">of Innovation</span>
            </h1>

            <p className="text-lg sm:text-xl text-gray-300 max-w-3xl mx-auto mb-8">
              Thank you for 4 incredible years. Get your free eSIM instantly with no registration, no login, and no plan selection required.
            </p>

            <div className="grid grid-cols-2 sm:grid-cols-4 gap-4 max-w-4xl mx-auto">
              {[
                { value: '4', label: 'Years Strong' },
                { value: '50M+', label: 'Users' },
                { value: '190+', label: 'Countries' },
                { value: '99.9%', label: 'Uptime' }
              ].map((stat, i) => (
                <div key={i} className="p-4 rounded-xl" style={{
                  background: 'rgba(255, 255, 255, 0.05)',
                  border: '1px solid rgba(255, 255, 255, 0.1)'
                }}>
                  <div className="text-3xl font-bold text-cyan-400">{stat.value}</div>
                  <div className="text-sm text-gray-400">{stat.label}</div>
                </div>
              ))}
            </div>
          </motion.div>

          {/* Free eSIM Section */}
          {!showInstructions ? (
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.6, delay: 0.2 }}
              className="max-w-4xl mx-auto"
            >
              <div className="p-8 sm:p-12 rounded-2xl" style={{
                background: 'linear-gradient(135deg, rgba(139, 92, 246, 0.1) 0%, rgba(236, 72, 153, 0.1) 100%)',
                border: '1px solid rgba(139, 92, 246, 0.2)',
                backdropFilter: 'blur(16px)'
              }}>
                <h2 className="text-3xl sm:text-4xl font-bold text-center mb-4">Get Your Free eSIM</h2>
                <p className="text-center text-gray-300 mb-8 text-lg">
                  No account required. No registration. No login. Choose your platform and activate instantly.
                </p>

                <div className="grid sm:grid-cols-2 gap-6 mb-8">
                  {/* iOS Option */}
                  <motion.button
                    whileHover={{ scale: 1.02 }}
                    whileTap={{ scale: 0.98 }}
                    onClick={() => handleGetESIM('ios')}
                    className="p-8 rounded-xl text-left transition-all"
                    style={{
                      background: 'rgba(255, 255, 255, 0.08)',
                      border: '2px solid rgba(0, 255, 255, 0.2)'
                    }}
                  >
                    <div className="w-16 h-16 rounded-2xl mb-4 flex items-center justify-center text-2xl font-bold"
                      style={{ background: 'rgba(0, 255, 255, 0.15)', color: '#00FFFF' }}>
                      iOS
                    </div>
                    <h3 className="text-2xl font-bold mb-2">iOS Install eSIM</h3>
                    <p className="text-gray-400 mb-4">For iPhone, iPad, and Apple Watch</p>
                    <div className="text-sm text-gray-500">
                      Requires iOS 12.1 or later
                    </div>
                  </motion.button>

                  {/* Android Option */}
                  <motion.button
                    whileHover={{ scale: 1.02 }}
                    whileTap={{ scale: 0.98 }}
                    onClick={() => handleGetESIM('android')}
                    className="p-8 rounded-xl text-left transition-all"
                    style={{
                      background: 'rgba(255, 255, 255, 0.08)',
                      border: '2px solid rgba(16, 185, 129, 0.2)'
                    }}
                  >
                    <div className="w-16 h-16 rounded-2xl mb-4 flex items-center justify-center text-2xl font-bold"
                      style={{ background: 'rgba(16, 185, 129, 0.15)', color: '#10B981' }}>
                      AND
                    </div>
                    <h3 className="text-2xl font-bold mb-2">Android Install eSIM</h3>
                    <p className="text-gray-400 mb-4">For Android smartphones and tablets</p>
                    <div className="text-sm text-gray-500">
                      Requires Android 9.0 or later
                    </div>
                  </motion.button>
                </div>

                {/* Alternative QR Method */}
                <div className="text-center pt-6 border-t border-white/10">
                  <button
                    onClick={() => setShowInstructions('qr')}
                    className="text-cyan-400 hover:text-cyan-300 transition-colors text-sm font-medium"
                  >
                    Or use Scan to Activate method
                  </button>
                </div>
              </div>
            </motion.div>
          ) : (
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              animate={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.6 }}
              className="max-w-4xl mx-auto"
            >
              {selectedPlatform === 'ios' && (
                <div className="p-8 sm:p-12 rounded-2xl" style={{
                  background: 'rgba(255, 255, 255, 0.05)',
                  border: '1px solid rgba(0, 255, 255, 0.2)',
                  backdropFilter: 'blur(16px)'
                }}>
                  <h2 className="text-3xl font-bold mb-6">iOS eSIM Installation</h2>
                  
                  <div className="mb-8 p-6 rounded-xl" style={{ background: 'rgba(0, 255, 255, 0.1)' }}>
                    <h3 className="text-xl font-semibold mb-4">Device Compatibility</h3>
                    <ul className="space-y-2 text-gray-300">
                      <li className="flex items-start gap-2">
                        <span className="text-cyan-400 mt-1">•</span>
                        <span>iPhone XS, XS Max, XR, and all newer models</span>
                      </li>
                      <li className="flex items-start gap-2">
                        <span className="text-cyan-400 mt-1">•</span>
                        <span>iPad Pro 11-inch and 12.9-inch (3rd generation and later)</span>
                      </li>
                      <li className="flex items-start gap-2">
                        <span className="text-cyan-400 mt-1">•</span>
                        <span>iPad Air (3rd generation and later)</span>
                      </li>
                      <li className="flex items-start gap-2">
                        <span className="text-cyan-400 mt-1">•</span>
                        <span>iPad Mini (5th generation and later)</span>
                      </li>
                      <li className="flex items-start gap-2">
                        <span className="text-cyan-400 mt-1">•</span>
                        <span>Apple Watch Series 3 and later (GPS + Cellular)</span>
                      </li>
                      <li className="flex items-start gap-2">
                        <span className="text-cyan-400 mt-1">•</span>
                        <span>iOS 12.1 or later required</span>
                      </li>
                    </ul>
                  </div>

                  <div className="mb-8">
                    <h3 className="text-xl font-semibold mb-4">Installation Steps</h3>
                    <div className="space-y-4">
                      {[
                        { step: 1, text: 'Ensure your device is connected to Wi-Fi' },
                        { step: 2, text: 'Tap the "Install eSIM Now" button below' },
                        { step: 3, text: 'Your device will open the eSIM setup screen automatically' },
                        { step: 4, text: 'Tap "Continue" to add the cellular plan' },
                        { step: 5, text: 'Wait for activation (usually takes 1-2 minutes)' },
                        { step: 6, text: 'Your eSIM is ready to use' }
                      ].map((item) => (
                        <div key={item.step} className="flex gap-4 p-4 rounded-lg" style={{ background: 'rgba(255, 255, 255, 0.03)' }}>
                          <div className="w-8 h-8 rounded-full flex items-center justify-center flex-shrink-0 font-bold"
                            style={{ background: 'rgba(0, 255, 255, 0.2)', color: '#00FFFF' }}>
                            {item.step}
                          </div>
                          <p className="text-gray-300 pt-1">{item.text}</p>
                        </div>
                      ))}
                    </div>
                  </div>

                  <div className="flex flex-col sm:flex-row gap-4">
                    <button
                      onClick={() => handleDirectInstall(IOS_PROVISION_URL)}
                      className="flex-1 btn btn-lg btn-primary"
                    >
                      Install eSIM Now
                    </button>
                    <button
                      onClick={() => setShowInstructions(false)}
                      className="btn btn-lg btn-secondary"
                    >
                      Back
                    </button>
                  </div>

                  <div className="mt-6 p-4 rounded-lg text-sm text-gray-400" style={{ background: 'rgba(255, 255, 255, 0.03)' }}>
                    <strong className="text-white">Note:</strong> This link will open your device's native eSIM installation interface. Make sure you're on the device where you want to install the eSIM.
                  </div>
                </div>
              )}

              {selectedPlatform === 'android' && (
                <div className="p-8 sm:p-12 rounded-2xl" style={{
                  background: 'rgba(255, 255, 255, 0.05)',
                  border: '1px solid rgba(16, 185, 129, 0.2)',
                  backdropFilter: 'blur(16px)'
                }}>
                  <h2 className="text-3xl font-bold mb-6">Android eSIM Installation</h2>
                  
                  <div className="mb-8 p-6 rounded-xl" style={{ background: 'rgba(16, 185, 129, 0.1)' }}>
                    <h3 className="text-xl font-semibold mb-4">Device Compatibility</h3>
                    <ul className="space-y-2 text-gray-300">
                      <li className="flex items-start gap-2">
                        <span className="text-green-400 mt-1">•</span>
                        <span>Google Pixel 3 and all newer Pixel models</span>
                      </li>
                      <li className="flex items-start gap-2">
                        <span className="text-green-400 mt-1">•</span>
                        <span>Samsung Galaxy S20, S21, S22, S23, S24 series</span>
                      </li>
                      <li className="flex items-start gap-2">
                        <span className="text-green-400 mt-1">•</span>
                        <span>Samsung Galaxy Z Fold and Z Flip series</span>
                      </li>
                      <li className="flex items-start gap-2">
                        <span className="text-green-400 mt-1">•</span>
                        <span>Motorola Razr (2019 and later)</span>
                      </li>
                      <li className="flex items-start gap-2">
                        <span className="text-green-400 mt-1">•</span>
                        <span>OPPO Find X3 Pro and newer flagships</span>
                      </li>
                      <li className="flex items-start gap-2">
                        <span className="text-green-400 mt-1">•</span>
                        <span>Android 9.0 (Pie) or later required</span>
                      </li>
                    </ul>
                  </div>

                  <div className="mb-8">
                    <h3 className="text-xl font-semibold mb-4">Installation Steps</h3>
                    <div className="space-y-4">
                      {[
                        { step: 1, text: 'Ensure your device is connected to Wi-Fi' },
                        { step: 2, text: 'Tap the "Install eSIM Now" button below' },
                        { step: 3, text: 'Your device will open Settings automatically' },
                        { step: 4, text: 'Tap "Download" to add the eSIM profile' },
                        { step: 5, text: 'Wait for activation (usually takes 1-2 minutes)' },
                        { step: 6, text: 'Enable the eSIM in your SIM settings' }
                      ].map((item) => (
                        <div key={item.step} className="flex gap-4 p-4 rounded-lg" style={{ background: 'rgba(255, 255, 255, 0.03)' }}>
                          <div className="w-8 h-8 rounded-full flex items-center justify-center flex-shrink-0 font-bold"
                            style={{ background: 'rgba(16, 185, 129, 0.2)', color: '#10B981' }}>
                            {item.step}
                          </div>
                          <p className="text-gray-300 pt-1">{item.text}</p>
                        </div>
                      ))}
                    </div>
                  </div>

                  <div className="flex flex-col sm:flex-row gap-4">
                    <button
                      onClick={() => handleDirectInstall(ANDROID_PROVISION_URL)}
                      className="flex-1 btn btn-lg"
                      style={{
                        background: 'linear-gradient(135deg, #10B981 0%, #059669 100%)',
                        color: 'white'
                      }}
                    >
                      Install eSIM Now
                    </button>
                    <button
                      onClick={() => setShowInstructions(false)}
                      className="btn btn-lg btn-secondary"
                    >
                      Back
                    </button>
                  </div>

                  <div className="mt-6 p-4 rounded-lg text-sm text-gray-400" style={{ background: 'rgba(255, 255, 255, 0.03)' }}>
                    <strong className="text-white">Note:</strong> This link will open your device's native eSIM installation interface. Make sure you're on the device where you want to install the eSIM.
                  </div>
                </div>
              )}

              {selectedPlatform === 'qr' && (
                <div className="p-8 sm:p-12 rounded-2xl" style={{
                  background: 'rgba(255, 255, 255, 0.05)',
                  border: '1px solid rgba(139, 92, 246, 0.2)',
                  backdropFilter: 'blur(16px)'
                }}>
                  <h2 className="text-3xl font-bold mb-6">Scan to Activate</h2>
                  
                  <div className="mb-8 p-6 rounded-xl text-center" style={{ background: 'rgba(139, 92, 246, 0.1)' }}>
                    <h3 className="text-xl font-semibold mb-4">QR Code Alternative</h3>
                    <p className="text-gray-300 mb-6">
                      If your device supports QR code scanning for eSIM activation, use the LPA string below:
                    </p>
                    <div className="p-4 rounded-lg font-mono text-sm break-all" style={{
                      background: 'rgba(0, 0, 0, 0.3)',
                      border: '1px solid rgba(139, 92, 246, 0.3)'
                    }}>
                      {ESIM_LPA_STRING}
                    </div>
                    <button
                      onClick={() => navigator.clipboard.writeText(ESIM_LPA_STRING)}
                      className="mt-4 btn btn-md btn-secondary"
                    >
                      Copy LPA String
                    </button>
                  </div>

                  <div className="mb-8">
                    <h3 className="text-xl font-semibold mb-4">Manual Installation Steps</h3>
                    <div className="space-y-4">
                      <div className="p-4 rounded-lg" style={{ background: 'rgba(255, 255, 255, 0.03)' }}>
                        <h4 className="font-semibold mb-2 text-cyan-400">For iOS:</h4>
                        <ol className="list-decimal list-inside space-y-2 text-gray-300 text-sm">
                          <li>Go to Settings → Cellular → Add Cellular Plan</li>
                          <li>Tap "Enter Details Manually"</li>
                          <li>Paste the LPA string above</li>
                          <li>Follow on-screen instructions</li>
                        </ol>
                      </div>
                      <div className="p-4 rounded-lg" style={{ background: 'rgba(255, 255, 255, 0.03)' }}>
                        <h4 className="font-semibold mb-2 text-green-400">For Android:</h4>
                        <ol className="list-decimal list-inside space-y-2 text-gray-300 text-sm">
                          <li>Go to Settings → Network & Internet → SIMs</li>
                          <li>Tap "Add" or "Download a SIM instead"</li>
                          <li>Select "Need help?" or "Enter it manually"</li>
                          <li>Paste the LPA string above</li>
                          <li>Follow on-screen instructions</li>
                        </ol>
                      </div>
                    </div>
                  </div>

                  <button
                    onClick={() => setShowInstructions(false)}
                    className="w-full btn btn-lg btn-secondary"
                  >
                    Back to Options
                  </button>
                </div>
              )}
            </motion.div>
          )}

          {/* Features Grid */}
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6, delay: 0.4 }}
            className="mt-20"
          >
            <h2 className="text-3xl font-bold text-center mb-12">Why Choose eSIM Myanmar</h2>
            <div className="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
              {[
                { title: 'Instant Activation', desc: 'Get connected in minutes, no physical SIM required', icon: 'IA' },
                { title: 'Global Coverage', desc: '190+ countries with seamless roaming', icon: 'GC' },
                { title: '5G Ready', desc: 'Ultra-fast 5G speeds where available', icon: '5G' },
                { title: 'VoLTE Support', desc: 'Crystal-clear HD voice calls', icon: 'HD' },
                { title: 'Multi-Device', desc: 'Use on phone, tablet, and watch', icon: 'MD' },
                { title: '24/7 Support', desc: 'Always here to help you', icon: '24' }
              ].map((feature, i) => (
                <div key={i} className="p-6 rounded-xl" style={{
                  background: 'rgba(255, 255, 255, 0.05)',
                  border: '1px solid rgba(255, 255, 255, 0.1)'
                }}>
                  <div className="w-12 h-12 rounded-lg mb-4 flex items-center justify-center font-bold"
                    style={{ background: 'rgba(0, 255, 255, 0.15)', color: '#00FFFF' }}>
                    {feature.icon}
                  </div>
                  <h3 className="text-xl font-semibold mb-2">{feature.title}</h3>
                  <p className="text-gray-400 text-sm">{feature.desc}</p>
                </div>
              ))}
            </div>
          </motion.div>

          {/* Support Section */}
          <motion.div
            initial={{ opacity: 0, y: 30 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6, delay: 0.6 }}
            className="mt-20 text-center"
          >
            <div className="p-8 rounded-2xl max-w-2xl mx-auto" style={{
              background: 'rgba(255, 255, 255, 0.05)',
              border: '1px solid rgba(255, 255, 255, 0.1)'
            }}>
              <h3 className="text-2xl font-bold mb-4">Need Help?</h3>
              <p className="text-gray-300 mb-6">
                Our support team is available 24/7 to assist you with installation and activation.
              </p>
              <div className="flex flex-col sm:flex-row gap-4 justify-center">
                <a href="mailto:info@esim.com.mm" className="btn btn-md btn-primary">
                  Email Support
                </a>
                <a href="tel:09650000172" className="btn btn-md btn-secondary">
                  Call 09650000172
                </a>
              </div>
            </div>
          </motion.div>
        </div>
      </div>
    </>
  );
};

export default Anniversary;
